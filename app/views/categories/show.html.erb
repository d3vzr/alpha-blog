<h1><%= "Category:" + @category.name %> </h1>

<div align="center">
  <span class="btn btn-info btn-xs  btn-raised"><%= link_to "Edit Category Name", edit_category_path(@category) %></span>
  <%= will_paginate @category_articles %>
</div>
<%= render 'articles/article', obj: @category_articles %>
<div align="center">
  <%= will_paginate @category_articles %>
</div>